/**
 * Simple function that returns the current epoch time for debugging.
 */
var _user$project$Native_GetCurrentTimeMillis = (function () {
    return {
        getCurrentTimeMillis: function() {
            return new Date().getTime();
        }
    };
})();
